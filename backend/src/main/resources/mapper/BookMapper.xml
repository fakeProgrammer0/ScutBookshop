<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.scut.bookshop.dao.BookDao">
    <select id="getBookDetail" resultType="BookDetail">
        select B.*, A.country_name as `author`
            from (select * from `book_t` where `id` = #{id})as B
         join `author_v` as A on B.author_id = A.id;
    </select>
</mapper>